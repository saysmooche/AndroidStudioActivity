<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bb.androidstudioactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".quiz_question2"></activity>
        <activity android:name=".PopQuiz_1" />
        <activity android:name=".content_providers" />
        <activity android:name=".Broadcast" />
        <activity android:name=".Content" />
        <activity android:name=".Services" />
        <activity android:name=".Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".Broadcast$MyBroadcastReceiver" />
    </application>

</manifest>